# Dependencies (reproducible via `npm install`)
node_modules/

# Generated test output (regenerated each run; contains live job data)
test/results.json

# Playwright browser profiles / logs
pw-profile/
*.log

# Saved job cards are personal — keep them out of git, but track the folder's README.
jobs-md/*.md
!jobs-md/README.md

# OS cruft
.DS_Store
Thumbs.db
desktop.ini
